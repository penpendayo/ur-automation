{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": [
    "@tsconfig/strictest/tsconfig.json"
  ],
  "compilerOptions": {
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "outDir": "outDirを指定しないとエラーになるので指定している",
    "lib": [
      "es2023"
    ],
    "target": "ES2022"
  }
}
